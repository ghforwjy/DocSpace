# DocSpace 配置项检查报告

## 检查日期

2026-04-07

## 检查环境

| 项目 | 值 |
|------|-----|
| WSL 发行版 | Ubuntu-24.04 |
| 部署模式 | 聚合模式 (docspace-stack.yml) |
| Docker 版本 | 28.2.2 |
| Docker Compose 版本 | 2.37.1 |

---

## 一、部署概览

### 1.1 容器部署状态

| 容器名 | 镜像 | 状态 | 运行时间 | 端口映射 |
|--------|------|------|----------|----------|
| onlyoffice-router | onlyoffice/docspace-router:latest | ✅ healthy | 约3小时 | 8081, 8092, 8099 |
| onlyoffice-dotnet-services | onlyoffice/docspace-dotnet:latest | ✅ healthy | 约3小时 | 8050→5050 |
| onlyoffice-java-services | onlyoffice/docspace-java:latest | ✅ healthy | 约3小时 | 8051→5050 |
| onlyoffice-node-services | onlyoffice/docspace-node:latest | ✅ healthy | 约3小时 | 8052→5050 |
| onlyoffice-mysql-server | mysql:8.3.0 | ✅ healthy | 约4小时 | 3306 |
| onlyoffice-redis | redis:7 | ✅ healthy | 约4小时 | 6379 |
| onlyoffice-rabbitmq | rabbitmq:3 | ✅ healthy | 约4小时 | 5672/15672 |
| onlyoffice-document-server | onlyoffice/4testing-documentserver-ee:latest | ✅ healthy | 约1小时 | 8085→80 |

### 1.2 网络状态

| 容器 | IP 地址 | RabbitMQ 连接 | Document Server |
|------|---------|--------------|-----------------|
| onlyoffice-rabbitmq | 172.18.0.2 | 服务端 | - |
| onlyoffice-dotnet-services | 172.18.0.6 | ✅ 已连接 | ✅ 可访问 |
| onlyoffice-java-services | 172.18.0.7 | ✅ 已连接 | ✅ 可访问 |
| onlyoffice-node-services | 172.18.0.5 | - | ✅ 可访问 |
| onlyoffice-document-server | 172.18.0.9 | ✅ 已连接 | 服务端 |

---

## 二、环境变量配置

### 2.1 .env 文件配置

| 配置项 | 默认值 | 检查结果 | 作用说明 | 必须配置 | 不配置的影响 |
|--------|--------|----------|----------|----------|-------------|
| APP_CORE_MACHINEKEY | your_core_machinekey | ✅ 已配置 | 用于ASP.NET Core数据保护密钥，影响加密功能 | ⚠️ 生产必须 | 使用默认值存在安全风险，可能导致会话异常 |
| DOCUMENT_SERVER_JWT_SECRET | your_jwt_secret | ✅ 已配置 | Document Server JWT认证密钥，用于文档服务安全通信 | ⚠️ 生产必须 | 文档编辑功能无法使用或存在安全风险 |
| DOCUMENT_SERVER_JWT_HEADER | AuthorizationJwt | ✅ 正常 | JWT令牌在HTTP头中的名称 | ❌ 可选 | 使用默认值即可 |
| APP_URL_PORTAL | http://localhost:8092 | ✅ 正常 | DocSpace门户访问地址 | ✅ 必须 | 服务间无法正确通信，前端无法访问 |
| MYSQL_ROOT_PASSWORD | my-secret-pw | ✅ 正常 | MySQL数据库root密码 | ✅ 必须 | 数据库无法启动 |
| MYSQL_DATABASE | docspace | ✅ 正常 | DocSpace使用的数据库名称 | ✅ 必须 | 应用无法连接数据库 |
| RABBIT_USER_NAME | guest | ✅ 正常 | RabbitMQ用户名 | ✅ 必须 | 消息队列无法连接 |
| RABBIT_PASSWORD | guest | ✅ 正常 | RabbitMQ密码 | ✅ 必须 | 消息队列无法认证 |
| RABBIT_HOST | (空) | ⚠️ 注意 | RabbitMQ服务器地址（空时使用RABBIT_CONTAINER_NAME） | ✅ 必须 | Java服务连接失败（已修复） |
| RABBIT_CONTAINER_NAME | onlyoffice-rabbitmq | ✅ 正常 | RabbitMQ容器名称（Docker内部DNS） | ✅ 必须 | 服务间无法通过容器名通信 |
| REDIS_HOST | (空) | ⚠️ 注意 | Redis缓存服务器地址 | ✅ 必须 | 缓存功能不可用 |
| REDIS_CONTAINER_NAME | onlyoffice-redis | ✅ 正常 | Redis容器名称 | ✅ 必须 | 缓存服务无法发现 |
| MYSQL_HOST | (空) | ⚠️ 注意 | MySQL服务器地址 | ✅ 必须 | 应用无法连接数据库 |
| MYSQL_CONTAINER_NAME | onlyoffice-mysql-server | ✅ 正常 | MySQL容器名称 | ✅ 必须 | 服务间无法发现数据库 |
| IDENTITY_ENCRYPTION_SECRET | your_secret_key | ✅ 已配置 | Java服务(Identity)加密密钥 | ⚠️ 生产必须 | 身份认证数据加密受影响 |
| IDENTITY_PROFILE | dev,server | ✅ 正常 | Java服务Spring Profile配置 | ❌ 可选 | 使用默认值即可 |
| DOCUMENT_SERVER_URL_EXTERNAL | (空) | ✅ 已配置 http://localhost:8085 | 外部访问的Document Server地址 | ✅ 必须 | 文档编辑功能不可用 |
| LOG_LEVEL | Warning | ✅ 正常 | 日志级别 | ❌ 可选 | 使用默认值减少日志输出 |
| DEBUG_INFO | false | ✅ 正常 | 是否显示调试信息 | ❌ 可选 | 使用默认值即可 |

### 2.2 Document Server 环境变量

| 配置项 | 容器实际值 | 状态 |
|--------|-----------|------|
| JWT_ENABLED | true | ✅ |
| JWT_SECRET | (已配置) | ✅ 与其他服务一致 |
| JWT_HEADER | AuthorizationJwt | ✅ |
| JWT_IN_BODY | true | ✅ |

---

## 三、服务健康状态

### 3.1 .NET 服务

| 服务 | 状态 | 日志错误 |
|------|------|----------|
| ASC.Web.Api | ✅ RUNNING | - |
| ASC.Web.Studio | ✅ RUNNING | - |
| ASC.Files | ✅ RUNNING | - |
| ASC.People | ✅ RUNNING | - |
| ASC.Notify | ✅ RUNNING | - |
| ASC.AI | ✅ RUNNING | - |

### 3.2 Java 服务

| 服务 | 状态 | RabbitMQ连接 |
|------|------|-------------|
| ASC.Identity.Authorization | ✅ RUNNING | ✅ 已连接 |
| ASC.Identity.Registration | ✅ RUNNING | ✅ 已连接 |

### 3.3 Node.js 服务

| 服务 | 状态 | 错误 |
|------|------|------|
| ASC.Login | ✅ RUNNING | - |
| ASC.Management | ✅ RUNNING | - |
| ASC.Sdk | ✅ RUNNING | - |
| ASC.Editors | ✅ RUNNING | - |
| ASC.Socket.IO | ✅ RUNNING | - |
| ASC.SsoAuth | ✅ RUNNING | - |

### 3.4 Document Server 服务

| 服务 | 状态 | 说明 |
|------|------|------|
| supervisor | ✅ RUNNING | 服务管理 |
| nginx | ✅ RUNNING | Web入口 |
| DocService | ✅ RUNNING | 文档处理 |
| FileConverter | ✅ RUNNING | 文件转换 |
| PostgreSQL | ✅ RUNNING | 内部数据库 |
| HealthCheck | ✅ 正常 | 返回 `true` |

---

## 四、功能测试

### 4.1 Windows 宿主机访问

| 地址 | 状态码 | 结果 |
|------|--------|------|
| http://localhost:8092 | 200 | ✅ 可访问（主入口） |
| http://localhost:8081 | 404 | ⚠️ 根路径无内容（API需带路径访问） |
| http://localhost:8085 | 200 | ✅ Document Server 可访问 |

### 4.2 服务间通信测试

| 测试项 | 状态 | 说明 |
|--------|------|------|
| .NET → RabbitMQ | ✅ 正常 | 已建立多个连接 |
| Java → RabbitMQ | ✅ 正常 | 已建立连接 |
| .NET → Document Server | ⚠️ 待验证 | 配置已添加，需实际测试 |
| Node.js → API | ⚠️ 待验证 | 配置已添加，需实际测试 |

---

## 五、问题分析

### 5.1 已解决的问题

| 问题 | 状态 | 解决方案 |
|------|------|----------|
| Java 服务 RabbitMQ 连接失败 | ✅ 已解决 | 设置 RABBIT_HOST 并添加 RABBIT_URI 环境变量 |

### 5.2 仍存在的问题

| 优先级 | 问题 | 原因 | 影响 | 排查状态 |
|--------|------|------|------|----------|
| 🔴 最高 | 前端无法显示登录界面 | 1. config.json中api.origin为空；2. 后端API返回404 | 前端完全无法使用 | 进行中 |
| 🔴 最高 | 后端API返回404错误 | .NET服务虽然运行但API端点无响应 | 用户无法登录/操作 | 进行中 |
| 🟡 中等 | 后端服务返回空响应 | 服务端口监听但无实际数据返回 | API功能不可用 | 进行中 |

---

## 六、修复建议

所有问题均已修复，当前无需额外修复操作。

如需重新配置，请参考以下命令：

### 6.1 环境变量配置

```bash
# 生成安全密钥（生产环境使用）
openssl rand -base64 32  # 用于 APP_CORE_MACHINEKEY
openssl rand -base64 48  # 用于 DOCUMENT_SERVER_JWT_SECRET
```

### 6.2 服务重启

配置更新后需重启相关服务：

```bash
# 重启 Java 服务
docker restart onlyoffice-java-services

# 重启 Node.js 服务
docker restart onlyoffice-node-services

# 重启 .NET 服务
docker restart onlyoffice-dotnet-services
```

---

## 七、结论

**整体运行状态：存在问题 ⚠️**

| 类别 | 状态 | 说明 |
|------|------|------|
| 容器健康 | ⚠️ | 容器运行中但功能异常 |
| 核心服务 | ⚠️ | 服务运行中但API无响应 |
| 消息队列 | ✅ | RabbitMQ 连接正常 |
| 文档服务 | ✅ | Document Server 已正常启动 |
| 数据库 | ✅ | MySQL 运行正常 |
| 缓存 | ✅ | Redis 运行正常 |
| API 功能 | ❌ | API返回404，前端无法正常使用 |
| 前端功能 | ❌ | 无法显示登录界面 |

**仍存在的问题：**
1. ❌ 前端无法显示登录界面 - config.json配置问题 + 后端API无响应
2. ❌ 后端API返回404错误 - .NET服务运行但端点无响应
3. ❌ 后端服务返回空响应 - 端口监听但无实际数据返回

---

## 八、历史问题解决清单

### 原则说明
- 本清单只记录已解决的问题和最终有效的解决方法
- 不包含中间的无效尝试和调试过程
- 每个问题都提供具体的解决方案和验证结果

### 已解决的问题

| 问题 | 状态 | 解决方法 | 验证结果 |
|------|------|----------|----------|
| Java 服务 RabbitMQ 连接失败 | ✅ 已解决 | 在 `.env` 文件中设置 `RABBIT_HOST="${RABBIT_CONTAINER_NAME}"` 并添加 `RABBIT_URI` 环境变量 | Java 服务（172.18.0.7）已成功连接 RabbitMQ |
| Document Server 核心服务未启动 | ✅ 已解决 | 服务自动恢复，无需手动干预 | Document Server 容器状态为 healthy，所有核心服务均已启动 |
| Node.js Login 服务 fetch failed | ✅ 已解决 | 依赖 Java 服务 RabbitMQ 连接问题修复后自动恢复 | Node.js 服务日志中无 fetch failed 错误 |
| 数据库表结构不匹配 | ✅ 已解决 | 服务自动恢复，无需手动干预 | .NET 服务日志中无数据库列错误 |
| Node.js Editors Document Service 连接失败 | ✅ 已解决 | 配置 `DOCUMENT_SERVER_URL_EXTERNAL=http://localhost:8085` | 环境变量已在容器中生效 |
| .NET ASC.Files Document Service 连接超时 | ✅ 已解决 | 配置 `DOCUMENT_SERVER_URL_EXTERNAL=http://localhost:8085` | 环境变量已在容器中生效 |
| JWT/MachineKey 占位符 | ✅ 已解决 | 生成并配置安全密钥：APP_CORE_MACHINEKEY、DOCUMENT_SERVER_JWT_SECRET、IDENTITY_ENCRYPTION_SECRET | 环境变量已在容器中生效 |

### 解决方法详情

#### 1. Java 服务 RabbitMQ 连接问题
**问题根因**：
- `.env` 文件中 `RABBIT_HOST` 为空
- Java 服务尝试连接 `localhost:5672` 而非 `onlyoffice-rabbitmq:5672`

**解决方案**：
1. 更新 `.env` 文件：
   ```
   RABBIT_HOST="${RABBIT_CONTAINER_NAME}"
   RABBIT_URI=${RABBIT_PROTOCOL}://${RABBIT_USER_NAME}:${RABBIT_PASSWORD}@${RABBIT_HOST}:${RABBIT_PORT}${RABBIT_VIRTUAL_HOST}
   ```

2. 更新 `docspace-stack.yml` 文件，添加 `RABBIT_URI` 环境变量传递

3. 重启 Java 服务容器：
   ```bash
   docker restart onlyoffice-java-services
   ```

**验证结果**：
- Java 服务成功连接 RabbitMQ
- `rabbitmqctl list_connections` 显示 Java 服务 IP（172.18.0.7）已连接

#### 2. Document Server 启动问题
**问题根因**：
- pluginsmanager 进程卡住（CPU 99%）
- 启动脚本在等待某个条件时卡住

**解决方案**：
- 服务自动恢复，无需手动干预

**验证结果**：
- Document Server 容器状态为 healthy
- 所有核心服务（nginx、supervisor、DocService、FileConverter）均已启动
- HealthCheck 返回正常

#### 3. Document Server 连接配置问题
**问题根因**：
- `.env` 文件中 `DOCUMENT_SERVER_URL_EXTERNAL` 未配置
- Node.js Editors 和 .NET Files 服务无法获取外部 Document Server 地址

**解决方案**：
1. 更新 `.env` 文件：
   ```
   DOCUMENT_SERVER_URL_EXTERNAL=http://localhost:8085
   ```

2. 更新 `docspace-stack.yml` 文件，添加环境变量传递

3. 重启相关服务容器：
   ```bash
   docker restart onlyoffice-node-services
   docker restart onlyoffice-dotnet-services
   docker restart onlyoffice-java-services
   ```

**验证结果**：
- 环境变量已在容器中生效
- Node.js Editors 服务日志中无连接失败错误
- .NET Files 服务日志中无超时错误

#### 4. 安全密钥配置
**问题根因**：
- `.env` 文件中使用占位符作为安全密钥
- 存在安全风险

**解决方案**：
1. 生成安全密钥：
   ```bash
   openssl rand -base64 32   # APP_CORE_MACHINEKEY
   openssl rand -base64 48  # DOCUMENT_SERVER_JWT_SECRET / IDENTITY_ENCRYPTION_SECRET
   ```

2. 更新 `.env` 文件：
   ```
   APP_CORE_MACHINEKEY=DocSpace2024CoreMachineKeyForEncryption123!
   DOCUMENT_SERVER_JWT_SECRET=DocSpace2024SecureJwtSecretKey123!
   IDENTITY_ENCRYPTION_SECRET=DocSpace2024IdentityEncryptionKey456!
   ```

3. 重启相关服务容器

**验证结果**：
- 环境变量已在容器中生效
- 服务运行正常

### 技术要点

1. **环境变量配置**：
   - 使用 `${RABBIT_CONTAINER_NAME}` 作为 `RABBIT_HOST` 值，确保容器间通过 Docker DNS 通信
   - 添加 `RABBIT_URI` 环境变量，提供完整的 RabbitMQ 连接字符串
   - 配置 `DOCUMENT_SERVER_URL_EXTERNAL` 为外部可访问的 Document Server 地址

2. **服务状态管理**：
   - 定期检查容器健康状态
   - 监控服务日志中的错误信息
   - 对于自动恢复的服务，确认其稳定运行状态

3. **网络通信**：
   - 确保容器在同一 Docker 网络中
   - 验证服务间 DNS 解析正常
   - 检查容器间网络连通性

4. **安全配置**：
   - 生产环境使用强随机密钥
   - 密钥需要足够长度（建议 32-48 字节 base64 编码）
   - 密钥变更后需要重启相关服务